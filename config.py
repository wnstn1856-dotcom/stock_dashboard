# -*- coding: utf-8 -*-
"""
설정 파일
- 국내/해외 종목 유니버스, 스코어링 가중치 등을 여기서 조정하세요.
- KR_TOP_N_BY_MARKETCAP / US_UNIVERSE_SIZE는 환경변수(KR_TOP_N / US_UNIVERSE_SIZE)로도 덮어쓸 수 있습니다.
  (GitHub Actions처럼 짧은 시간 안에 자주 도는 자동 실행 환경에서 종목 수를 줄이는 용도)
"""
import os

# ------------------------------------------------------------------
# 국내 주식 설정
# ------------------------------------------------------------------
KR_MARKETS = ["KOSPI", "KOSDAQ"]      # 조회할 국내 시장
KR_TOP_N_BY_MARKETCAP = int(os.environ.get("KR_TOP_N", 300))
                                        # 시장별 시가총액 상위 N종목 (KOSPI+KOSDAQ 합쳐 최대 600종목)
                                        # 종목이 많을수록 정확도는 오르지만 실행 시간이 늘어남

# ------------------------------------------------------------------
# 해외(미국) 주식 설정
# ------------------------------------------------------------------
# 1) 가능하면 위키피디아에서 S&P500 구성종목을 자동으로 가져오고,
# 2) 실패하면 아래 curated list를 사용합니다.
US_UNIVERSE_SIZE = int(os.environ.get("US_UNIVERSE_SIZE", 200))
                                # 위키피디아에서 몇 종목을 가져올지 (앞에서부터 N개, S&P500 최대 500)
US_FALLBACK_TICKERS = [
    # 빅테크 / AI
    "AAPL", "MSFT", "GOOGL", "AMZN", "META", "NVDA", "TSLA", "AVGO", "ORCL", "CRM",
    "ADBE", "AMD", "QCOM", "INTC", "PLTR", "NOW", "IBM", "CSCO", "TXN", "MU",
    # 반도체 / 하드웨어
    "TSM", "ASML", "AMAT", "LRCX", "KLAC",
    # 금융
    "JPM", "BAC", "WFC", "GS", "MS", "V", "MA", "AXP", "BRK-B", "SCHW",
    # 헬스케어 / 바이오
    "LLY", "UNH", "JNJ", "PFE", "MRK", "ABBV", "TMO", "ISRG",
    # 소비재 / 리테일
    "WMT", "COST", "HD", "MCD", "NKE", "SBUX", "PG", "KO", "PEP", "DIS",
    # 에너지 / 산업재
    "XOM", "CVX", "CAT", "BA", "GE", "HON",
    # 커뮤니케이션 / 미디어
    "NFLX", "CMCSA", "T", "VZ",
    # 전기차 / 신재생
    "RIVN", "ENPH", "FSLR",
    # 기타 성장주
    "SHOP", "UBER", "ABNB", "SNOW", "PANW", "CRWD", "DDOG", "NET", "COIN", "SOFI",
]

# ------------------------------------------------------------------
# 스코어링 가중치 (0~1, 합이 1이 되지 않아도 내부적으로 정규화됨)
# ------------------------------------------------------------------
WEIGHT_VALUATION = 0.5   # PER, PBR 등 저평가 지표 가중치
WEIGHT_MOMENTUM = 0.5    # 등락률, 거래량 급증 등 모멘텀 가중치

# 상위 N개 추천 종목 표시 (콘솔 출력용)
TOP_N_RECOMMEND = 20

# ------------------------------------------------------------------
# 기술적 지표 설정
# ------------------------------------------------------------------
RSI_PERIOD = 14
MACD_FAST = 12
MACD_SLOW = 26
MACD_SIGNAL = 9
# MACD가 안정적으로 수렴하려면 최소 slow+signal(기본 35) 거래일 이상의 데이터가 필요.
# 여유 있게 더 긴 기간의 시세를 가져온다.
KR_HISTORY_DAYS = 150      # 국내: 최근 N 캘린더일치 시세 조회
US_HISTORY_PERIOD = "9mo"  # 해외: yfinance 조회 기간

# ------------------------------------------------------------------
# 뉴스 수집 설정
# ------------------------------------------------------------------
FETCH_NEWS = True         # False로 하면 뉴스 수집을 건너뛰어 훨씬 빨라짐
NEWS_MAX_ITEMS = 5         # 종목당 확인할 최신 헤드라인 개수
NEWS_SLEEP_SEC = 0.15      # 국내 뉴스 스크래핑 시 요청 간 대기 시간 (네이버 서버 부담 완화용)

# ------------------------------------------------------------------
# 매수/매도 신호(rule-based, 참고용) 판단 기준
# ------------------------------------------------------------------
OVERSOLD_RSI = 30           # RSI 이 값 이하면 "과매도"
OVERBOUGHT_RSI = 70         # RSI 이 값 이상이면 "과매수"
VOLUME_SURGE_MULT = 2.0     # 평소(20일 평균) 대비 거래량이 이 배수 이상이면 "거래량급증"
UNDERVALUED_PERCENTILE = 70  # 밸류에이션 점수(0~100)가 이 값 이상이면 "저평가"

# 매수 신호 후보: 과매도 / 저평가 / 거래량급증 / MACD 골든크로스 / 뉴스 호재 (총 5개 중)
BUY_SIGNAL_MIN_COUNT = 3
# 매도 신호 후보: 과매수 / MACD 데드크로스 / 뉴스 악재 (총 3개 중)
SELL_SIGNAL_MIN_COUNT = 2

# 데이터 저장/출력 경로
OUTPUT_DIR = "."
